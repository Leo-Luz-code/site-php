propel:
  database:
    connections:
      default:
        adapter: mysql
        dsn: mysql:host=localhost;port=3306;dbname=site_php
        user: root
        password:
        settings:
          charset: utf8
  runtime:
    defaultConnection: default
    connections:
      - default
